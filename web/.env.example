# Production API URL (e.g., https://your-api-server.railway.app)
# If left empty, it will default to "/api" (suitable for same-domain deployment or proxy)
VITE_API_URL=http://localhost:4000
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
